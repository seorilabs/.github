# LEGACY PATH - intentionally contains no Kubernetes object.
# Production NetworkPolicy resources are generated together by
# ../../scripts/render-production-k8s.mjs. Keeping this path as a comment-only
# compatibility marker prevents stale placeholder deployment.
